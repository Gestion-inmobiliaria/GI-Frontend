export interface IClient {
    id: string
    name: string
    email: string
    phone: string
    address?: string
    documentType: string
    documentNumber: string
    createdAt: string
    updatedAt: string
}
